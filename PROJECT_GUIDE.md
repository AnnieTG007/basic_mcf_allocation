# 项目约束

本文是本项目规范的唯一权威来源。修改代码、配置、文档或清理文件前必须完整阅读并遵守。`AGENTS.md` 仅链接到本文，不维护第二套规则。

## 不保留测试

- 项目所有者明确不需要任何测试：不创建、不保留测试目录、测试脚本、测试用例、测试框架依赖或测试 CI，也不要求运行测试套件。
- 必要的运行核查使用正式 `main.py` 入口，检查仿真是否完成及输出是否合理；这不应衍生出独立验证脚本。核查产生的临时文件在完成后清理。

## 注释与入门文档

- 注释面向初次接触项目的研究生，使用直白中文。首次出现的缩写解释含义；避免只写“初始化”“处理数据”等无法说明实际工作的注释。
- 每个脚本开头说明职责、如何被调用、主要输入输出及单位。重要函数说明数组各维含义、返回值、边界情况；算法说明选择顺序和同分处理；统计说明窗口、分母和单位。
- 解释为什么这样计算，不逐行翻译语法；删除失效的注释和注释掉的调试代码。已有物理公式无法确认来源或单位时如实标明，不猜测、不借注释整理改变公式。
- 注释验收方式：让不携带项目记忆、历史会话或背景解释的子智能体，只读当前文件，复述模块职责、关键规则和限制；根据误读或缺口修正注释。
- `README.md` 只保留项目用途、安装、最少运行示例、输出位置和脚本导航。算法、数据格式及统计细节写在对应脚本注释中；不保留本机路径、过期验证数字或失效结果链接。

## 脚本命名规则

- Python 文件统一使用小写下划线命名（snake_case），缩写也使用小写，例如 `skr_calculation.py`。
- 模块名按职责使用名词或名词短语：计算模块使用 `*_calculation.py`，纤芯布局使用 `core_layout.py`，业务模块使用 `traffic_*`。避免含义模糊的 `*_code.py`。
- `main.py` 保留为命令行入口；文件重命名时同步修改导入、当前运行的源码清单、IDE 运行配置和文档引用。

## 算法统一放置

- **所有资源分配算法必须统一实现在 `algorithm.py`，仅保留 CQLI、CCA、FF（first-fit）、SCWA、GREEDY_MIN_NOISE。** CQLI、CCA、FF 对齐参考项目 `KeyConsumption_24node -7 core` 的动态算法，SCWA 保留参考分组并修复可变信道数下的容量浪费，其中 `my` 即 CQLI；保留本项目现有频点和量子预留配置。四者均按实际频率从低到高搜索。FF 量子芯 6、经典芯 [0,1,2,3,4,5] 双向共享，按参考搜索函数的芯编号升序搜索；CCA 量子芯 0、经典芯按列表 [1,2,3,4,5,6] 双向共享并搜索。同频时 FF 按芯编号升序，CCA 保持配置列表顺序；两者禁止反向同芯同频同时占用。CQLI 量子芯 6、前向芯 0/2/4、后向芯 1/3/5。SCWA 量子芯 1、前向芯 4/3/5、后向芯 6/0/2，按参考奇偶分组，以原首选集合的有效经典位置占用率达到 7/12（参考名义阈值 14/24）时交换偏好；状态 0/3 不计入占用率。先找全路径首选分配，失败后放开互补集合，逐跳保留偏好，不因奇偶限制单独阻塞。奇偶序号仍按全部实际频率升序从零编号。该 SCWA 是适配修复版，不再声称与参考固定余量 10、无回退的实现完全一致。三芯硬件回放是独立实验约束，FF 在该模式固定前向芯 0/1/2、后向芯 3/4/5，不冒充参考单芯 FF。CCA 三芯实验适配固定量子芯0、前向芯[1,2,3]、后向芯[4,5,6]，按实际频率升序选择可行三芯组，不冒充参考六芯双向共享 CCA；实验导出 ALL 包含 FF、CCA、GREEDY_MIN_NOISE。FF 仍为协同度基准；协同度保留平方根幅值，仅当 OSNR 和 SKR 相对 FF 均严格提高时为正，否则非零幅值取负；任一指标相等时幅值为零，不限幅。按同工况同种子配对赋号后跨种子平均。量子侧归一化上限为实际距离下的零外加噪声 SKR，下限为受到（经典纤芯数-1）个正向芯间串扰源时的 SKR；经典芯按前后向并集计数，使用本次功率、量子频率及有限样本/探测参数，每源耦合系数为1e-6 km^-1。上下限重合时协同度留空。经典侧使用本次每芯每信道发射功率、六个正向串扰源、固定噪声底的单跳 OSNR 标尺。算法清单统一使用 `ALGORITHMS`，不保留 MAX_SKR、CQLI_NOISE 或静态精确基准实现，不创建独立算法脚本。
- 在线算法通过 `ResourceAllocator` 选择和调用。算法接收当前路径、资源、功率和距离，返回分配结果；实际资源占用、释放及业务事件由 `main.py` 管理。
- 算法专用的候选排序、FWM 安全检查、回退及破同分逻辑留在 `algorithm.py`。物理噪声由 `noise_calculation.py` 负责；BB84 参数与公式、网络 SKR 汇总、量子链路评分和缓存统一由 `skr_calculation.py` 负责，不把资源搜索混入评分模块。
- 结构整理必须保持算法规则、候选顺序、方向分组、实际频率、随机种子、物理公式和统计口径。移动实现时同步更新调用方、源码哈希清单和文档，并通过正式入口核查结果。

## 正式仿真与模块职责

- `main.py`：唯一命令行入口、实例构建和业务事件仿真。`ClassicalService.iter_slots()` 是唯一事件循环；单次 `run()` 和扫描测量都复用它，观察回调不修改资源。
- `traffic_scan.py`：由 `main.py --scan-load/--scan-power/--scan-distance/--scan-all` 或 `--export-business` 调用，负责负载/功率/距离实验编排、配对业务校验、窗口内采样、跨种子统计和统一源码哈希清单；通过实例的 `iter_slots()` 观察事件，不另写事件循环，不反向导入 `main.py`。
- `traffic_export.py`：记录资源状态变化、序列化回放与扫描 JSON、生成 Excel 和图表；仅接收实例状态或已统计的结果，不生成业务、不做分配决策、不编排扫描，不反向导入 `main.py` 或 `traffic_scan.py`。
- `algorithm.py`：全部资源分配算法实现。
- 扫描与业务图仅标注 GREEDY_MIN_NOISE 相对 CCA 的最大 SKR 绝对百分比差（|SKR/CCA-1|，CCA为零时跳过）及最大 OSNR 绝对差对应的带符号 dB 差。Excel 使用普通黑白单元格，只含必要条件、算法、SKR/OSNR/阻塞率/协同度及四项各自相对FF的比值；OSNR比值使用线性值，基准为零或缺失时留空，不附加配置/样本页或图表，完整数据保留在JSON。
- `topology.py`、`core_layout.py`、`topologies/`：拓扑、路由与纤芯邻接数据。

正式实验应复用上述入口。不要为某次探索、消融或固定结论新增长期保留的 `analyze_*`、`benchmark_*`、`validate_*`、`report_*` 阶段脚本；可复用且正式仿真需要的能力应合入对应模块。

## 结果与临时文件

- `results/` 仅在本地保存实验结果，因频繁更换结果，整个目录不纳入 Git 提交；已有跟踪结果从版本控制中移除，仍存在的本地文件保留。目录中保存最终结论、汇总、图表，以及支撑结论所需的配置、种子和原始数据。最终扫描的逐时隙数据是实验依据，不按临时代码清理。项目所有者不需要保留历史哈希数据，已有批次中的源码、文件和业务序列哈希可直接清理，不将其作为发布必需资料。
- 已完成使命、且正式仿真不再依赖的探索、调试、报告拼装脚本应删除。缓存、未完成输出、临时验证文件不作为交付内容保留。
- 历史结果按生成时的配置解释，不把旧数据当成新版本实测。历史配置不同的结果不得直接混算增益。
- 报告中的运行说明必须与现有入口一致；删除生成脚本后，标明报告为结论快照，不能继续提供已失效的复现命令。

## 验证要求

- 修改后运行与改动相关的正式 `main.py` 命令，检查运行完成、配置、结果和导出文件。
- 涉及算法迁移时，以固定配置、种子对比迁移前后的分配结果或仿真指标；不通过重写基准数据掩盖差异。
- 默认使用 topology7 的两节点链路（0–1），距离直接读取拓扑文件，每条业务独立等概率选择0→1或1→0，不自动生成反向配对业务；负载为两个方向合计。普通运行默认30 Erlang（到达率7.5、保持时间4），普通扫描默认负载[30]；三芯实验导出的负载组默认[5,10,15,20,25,30,35,40] Erlang业务组，保持时间4，到达率为负载除以4；功率扫描固定负载默认10（到达率2.5）。三芯组显式负载不再除以三，不为满足阻塞率目标自动降载，也不保证任意配置均低于5%。显式选择其他拓扑时，在所选完整网络生成业务；默认使用拓扑文件中各边的实际 length_km，不进行缩放，并观测最短边，同长时按节点编号字典序选择。--observe-link只选择观测链路，不限制源宿或改变长度；显式全边等长/单边覆盖用于另行指定的实验，单边覆盖优先。SKR采用用户提供的SKR_new.py中双诱骗态有限样本估算，逐量子信道截零后对链路、时隙平均；raw_skr仅作有效界下截零前差值诊断，不参与正式SKR、增益或协同度。默认总脉冲数1e10、波动系数5.3、信号/诱骗强度0.6/0.2、概率14/16和1/16，其余为真空态；保留项目探测和损耗参数。每个资源快照按固定静态脉冲块估算，脉冲块时长与仿真时隙无换算关系；这是高斯统计波动近似，不声称完整可组合安全。输出必须记录模型版本和实际参数。OSNR使用参考业务入口公式的单链路版本（实际长度、实际功率、一跳、串扰加固定噪声底），不计入芯内FWM和SpRS；空闲时隙不参与OSNR平均。单次与扫描复用measure_run，比较时保持相同窗口。

## GitHub 发布

- `.gitignore` 排除缓存、IDE 配置、本地环境及临时输出，并整体忽略本地实验结果目录 `results/`。
- 发布前核对依赖、文档链接、隐私信息、文件大小及结果文件完整性。清理历史哈希时同步处理 JSON 与 Excel 中的记录，不改动实验参数和统计值。
- 许可证由项目所有者决定；第三方代码和数据的来源及授权应据实说明，不自行猜测作者、引用或许可。
- 项目所有者已在 GitHub 仓库创建 MIT `LICENSE`；首次合并保留远端该文件，不另建副本或覆盖其中的署名。
