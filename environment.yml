# Run from the project root. This is a rebuild recipe, not a full environment lock.
name: mcf-resource-allocation
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - numpy==2.4.3
      - pandas==3.0.1
      - networkx==3.6.1
      - xlrd==2.0.2
      - matplotlib>=3.8,<4
      - openpyxl>=3.1,<4
